[project]
name = "zakuro"
version = "1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiosql>=15.0",
    "click",
    "patool",
    "pyinstaller>=6.22.2",
    "python-magic>=0.4.27",
]

[project.scripts]
zakuro = "main:main"
